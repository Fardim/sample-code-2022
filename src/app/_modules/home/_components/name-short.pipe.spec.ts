import { NameShortPipe } from './name-short.pipe';

describe('NameShortPipe', () => {
  it('create an instance', () => {
    const pipe = new NameShortPipe();
    expect(pipe).toBeTruthy();
  });
});
